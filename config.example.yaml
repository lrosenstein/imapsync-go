src:
  label: old
  server: mail.oldhost.com:993
  user: user@oldhost.com
  pass: password

dst:
  label: new
  server: imap.newhost.com:993
  user: user@newhost.com
  pass: password

map:
  - src: INBOX
    dst: INBOX
    no_expand_subfolders: true
  - src: Sent
    dst: Sent
  - src: Archive.2023
    dst: Archive/2023

# Optional throttle. Zero / omitted means unlimited.
# For Gmail accounts, set both to 300000 to stay safely under the
# 2.5 GB/day download and 500 MB/day upload IMAP quotas, and cap
# max_connections at ~10 (Gmail's hard cap is 15 simultaneous IMAP
# sessions per account).
rate_limit:
  down_bps: 0
  up_bps: 0
  max_connections: 0
